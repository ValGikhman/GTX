@model GTX.Models.ContactModel
@{
    Html.RenderPartial("_PageTitle");
}

<div class="container-fluid d-flex justify-content-center">
    <div class="well row mt-1 p-3 rounded shadow col-lg-7">
        @foreach (var open in Model.OpenHours) {
            <div class="col">
                <button type="button" class="btn btn-secondary shadow position-relative">
                    @open.Day
                    <span class="position-absolute top-0 start-100 translate-middle shadow badge rounded bg-dark">
                        @open.Description
                    </span>
                </button>
            </div>
        }
    </div>
</div>

<div class="container-fluid d-flex justify-content-center">
    <div class="well row mt-3 p-3 rounded shadow">
        <div class="col-lg-6">
            <img class="w-50 rounded-2 shadow" src="@Url.Content("~/img/gtx.jpg")">
            <h3 class="card-title mt-3">Firfield Showroom</h3>
            <p class="card-text">
                9516 PRINCETON GLENDALE RD, West Chester, OH 45011<br/>
                (513) 912-4574
            </p>
        </div>
        <div class="col-lg-6">
            @{ Html.RenderPartial("_ContactForm", Model.Contact); }
        </div>
    </div>
</div>

<script>
    (function () {
        $("#btnClose").hide();
    })()
</script>
