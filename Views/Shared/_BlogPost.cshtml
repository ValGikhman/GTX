@model List<GTX.Models.BlogPostModel>
<link href="~/Content/majordome.css?v=@(System.IO.File.GetLastWriteTimeUtc(Server.MapPath("~/Content/majordome.css")).Ticks)" rel="stylesheet" />

<div class="shadow rounded-4 overflow-hidden">

    <div class="card-header bg-white p-3 border-0 sticky-top" style="z-index: 1020;">
        <div class="d-flex align-items-center justify-content-end">
            <a id="newBlog" class="btn btn-gradient"
               data-blog-modal="true"
               data-title="New Blog Post"
               href="@Url.Action("Create", "Blogs")">
                <i class="bi bi-plus-lg me-1"></i> New
            </a>
        </div>
    </div>

    <div class="card-body p-0">
        <div id="blogsAddGrid" class="gtx-admin-add-grid"></div>
        <div class="d-none" aria-hidden="true">
            <table id="bpTableSource">
                <thead class="table-light">
                    <tr>
                        <th style="min-width:90px;">Image</th>
                        <th style="min-width:160px;">Title</th>
                        <th style="min-width:160px;">Author</th>
                        <th style="width:120px;">Published</th>
                        <th style="min-width:70px;">Created</th>
                        <th class="text-end" style="width:160px;">Actions</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model == null || !Model.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center p-4 text-muted">
                                No posts yet. Click <strong>New</strong> to create one.
                            </td>
                        </tr>
                    }
                    else
                    {
                        foreach (var p in Model)
                        {
                            <tr data-grid-row="true" data-published="@(p.IsPublished ? "1" : "0")">
                                <td>
                                    <img width="90" height="60" class="rounded-2 border"
                                         src="@($"{p.MediaURL}?h=60")" />
                                </td>

                                <td>
                                    <div class="fw-semibold">@p.Title</div>
                                </td>

                                <td>@(string.IsNullOrWhiteSpace(p.Author) ? "N/A" : p.Author)</td>

                                <td>
                                    @if (p.IsPublished)
                                    {
                                        <span class="badge rounded-pill bg-success-subtle text-success border border-success-subtle shadow">
                                            Published
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge rounded-pill bg-secondary-subtle text-secondary border border-secondary-subtle">
                                            Draft
                                        </span>
                                    }
                                </td>

                                <td class="text-muted">
                                    @p.CreatedAt.ToString("yyyy-MM-dd")
                                </td>

                                <td class="text-end">
                                    <div class="btn-group">
                                        <a class="btn btn-gradient-grey btn-sm btn-outline-primary"
                                           data-blog-modal="true"
                                           data-title="Edit Blog Post"
                                           href="@Url.Action("Edit", "Blogs", new { id = p.Id })">
                                            <i class="bi bi-pencil-square me-1"></i>
                                        </a>

                                        <button id="btnDelete-@p.Id"
                                                type="button"
                                                class="btn btn-gradient-grey btn-sm btn-outline-danger"
                                                onclick="deleteBlog(@p.Id)"
                                                data-id="@p.Id"
                                                title="Delete">
                                            <i class="bi bi-trash me-1"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>

</div>

<script type="text/x-addgrid-template" id="blog-grid-image-template">{{ImageHtml}}</script>
<script type="text/x-addgrid-template" id="blog-grid-title-template">{{TitleHtml}}</script>
<script type="text/x-addgrid-template" id="blog-grid-author-template">{{AuthorHtml}}</script>
<script type="text/x-addgrid-template" id="blog-grid-published-template">{{PublishedHtml}}</script>
<script type="text/x-addgrid-template" id="blog-grid-created-template">{{CreatedHtml}}</script>
<script type="text/x-addgrid-template" id="blog-grid-actions-template">{{ActionsHtml}}</script>

<script>
    async function deleteBlog(id) {
        if (!id) return;

        const confirmed = await window.gtxConfirm({
            title: "Delete blog post",
            message: "Delete this blog post? This cannot be undone.",
            confirmText: "Delete",
            variant: "danger"
        });
        if (!confirmed) return;

        $.post(`${root}Blogs/Delete`, { id: id })
            .done(function (res) {
                if (res && res.ok === true) {
                    console.log("Deleted");
                    refreshList();
                    return;
                }
                window.gtxAlert("Delete failed.");
            })
            .fail(function (xhr) {
                window.gtxAlert("Delete failed. Status: " + xhr.status);
            });
    };

    function refreshList() {
        const $grid = $("#blogsAddGrid");
        window.gtxAdminGridState = window.gtxAdminGridState || {};
        window.gtxAdminGridState.blogsSearch = $grid.data("add-grid")
            ? $grid.addGrid("getSearch")
            : (window.gtxAdminGridState.blogsSearch || "");
        const listContainerSelector = "#tab-blog";

        $(listContainerSelector).load(`${root}Blogs/ListPartial`);
    }

    $(function initializeBlogsAddGrid() {
        const $source = $("#bpTableSource");
        const rows = $source.find("tbody tr[data-grid-row='true']").map(function () {
            const $cells = $(this).children("td");
            const title = $.trim($cells.eq(1).text());
            const author = $.trim($cells.eq(2).text());
            const published = $.trim($cells.eq(3).text());
            const created = $.trim($cells.eq(4).text());

            return {
                ImageState: $cells.eq(0).find("img").length ? "Has Image" : "No Image",
                Title: title,
                Author: author,
                Published: published,
                Created: created,
                SearchText: [title, author, published, created].join(" "),
                ImageHtml: $cells.eq(0).html(),
                TitleHtml: $cells.eq(1).html(),
                AuthorHtml: $cells.eq(2).html(),
                PublishedHtml: $cells.eq(3).html(),
                CreatedHtml: $cells.eq(4).html(),
                ActionsHtml: $cells.eq(5).html()
            };
        }).get();

        $source.parent().remove();
        window.gtxAdminGridState = window.gtxAdminGridState || {};

        const $grid = $("#blogsAddGrid");
        $grid.addGrid({
            data: rows,
            columns: [
                { field: "ImageState", title: "Image", width: 110, filterable: false, searchable: false, template: "#blog-grid-image-template" },
                { field: "Title", title: "Title", width: 230, filterable: false, searchable: true, searchField: "SearchText", template: "#blog-grid-title-template" },
                { field: "Author", title: "Author", width: 180, searchable: false, template: "#blog-grid-author-template" },
                { field: "Published", title: "Published", width: 130, searchable: false, headerClass: "text-center", cellClass: "text-center", template: "#blog-grid-published-template" },
                { field: "Created", title: "Created", width: 130, filterable: false, searchable: false, headerClass: "text-center", cellClass: "text-center text-muted", template: "#blog-grid-created-template" },
                { field: "Actions", title: "Actions", width: 140, filterable: false, searchable: false, headerClass: "text-center", cellClass: "text-center", template: "#blog-grid-actions-template" }
            ],
            tableClass: "table align-middle m-0",
            height: null,
            pageable: false,
            showRecordCount: true,
            recordType: { singular: "blog", plural: "blogs", icon: "bi bi-newspaper" },
            sortable: false,
            filterable: true,
            resizable: false,
            reorderable: false,
            searchable: true,
            showSearch: true,
            searchTerm: window.gtxAdminGridState.blogsSearch || "",
            searchPlaceholder: "Search title or author…",
            showFilterChips: true,
            exportToExcel: false,
            exportToPdf: false,
            groupable: false,
            alternateRows: true,
            emptyText: "No blog posts match the current filters.",
            emptyHint: "Adjust the search or column filters to see more posts.",
            onSearchChange: function (detail) {
                window.gtxAdminGridState.blogsSearch = detail.term || "";
            }
        });

        const $newButton = $("#newBlog").detach().addClass("btn-sm");
        $grid.find(".pg-search-input-group").after($newButton);
        $grid.closest(".shadow").children(".card-header").remove();
    });
</script>
